
Use cicdpipeline;
-- Create the 'filters' table
CREATE TABLE chomu (
                         id varchar(50),
                         name varchar(50)
);

-- Add a new column 'phone_number' to the 'demo' table (if it exists)
ALTER TABLE demo ADD COLUMN diddy varchar(50);
